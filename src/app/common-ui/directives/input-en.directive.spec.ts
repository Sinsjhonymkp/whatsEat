import { InputEnDirective } from './input-en.directive';

describe('InputEnDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEnDirective();
    expect(directive).toBeTruthy();
  });
});
